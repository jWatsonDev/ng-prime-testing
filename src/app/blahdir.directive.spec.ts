import { BlahdirDirective } from './blahdir.directive';

describe('BlahdirDirective', () => {
  it('should create an instance', () => {
    const directive = new BlahdirDirective();
    expect(directive).toBeTruthy();
  });
});
